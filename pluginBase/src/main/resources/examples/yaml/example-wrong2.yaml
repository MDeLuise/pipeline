---
- triggers:
    - name: perTrue
  processors:
    - type: filter
      name: eq
      options:
        compareValue: 42
    - type: action
      name: print
      options:
        text: "### LOG: %s"
